// Stub matcher.js for Alt1
window.captureRs = async () => await alt1.getRegion();
window.loadImage = async (src) => await a1lib.ImageData.fromUrl(src);
window.findAnchor = (img, anchor, opts) => img.findSubimage(anchor, opts);
